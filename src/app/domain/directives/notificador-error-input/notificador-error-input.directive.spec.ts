import { NotificadorErrorInputDirective } from './notificador-error-input.directive';

describe('NotificadorErrorInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificadorErrorInputDirective();
    expect(directive).toBeTruthy();
  });
});
